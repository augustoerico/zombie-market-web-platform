var React = require('react');

var ProductForm = React.createClass({
    render: function () {
        return (
            <div>
                <h1>ProductForm</h1>
            </div>
        );
    }
});

module.export = ProductForm;
