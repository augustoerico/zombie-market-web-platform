var React = require('react');

var ProductList = React.createClass({
    render: function () {
        return (
            <div>
                <h1>ProductList</h1>
            </div>
        );
    }
});

module.export = ProductList;
